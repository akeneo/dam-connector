version: '3'

services:
    pim-api-test_php-cli:
        build: .
        environment:
            COMPOSER_HOME: /tmp
        user: ${UID:-1000}:${GID:-1000}
        volumes:
            - ${COMPOSER_HOME:-~/.composer}:/tmp
            - .:/srv/pim-api-test
        working_dir: /srv/pim-api-test
